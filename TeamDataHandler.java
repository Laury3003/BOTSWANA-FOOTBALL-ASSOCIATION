// TeamDataHandler.java
// Abstract base class that holds a shared reference to an array of teams.
// Implements the TeamManager interface which defines operations related to team management.

public abstract class TeamDataHandler implements TeamManager {
    protected Team[] teams;
}
